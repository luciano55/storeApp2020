package com.example.demo.validate.old;

/*
 * import harnina.entity.User; import harnina.entity.UserData; import
 * harnina.errror.ErrorValidate; import harnina.util.AddErrorArrayError; import
 * com.example.demo.entity.Client; import
 * com.example.demo.util.AddErrorArrayError;
 * 
 * import java.util.ArrayList; import java.util.HashMap;
 * 
 * public class ValidateClientFacade {
 * 
 * public static HashMap<String, ArrayList<ErrorValidator>> getErrorsAll(Client
 * client) { HashMap<String, ArrayList<ErrorValidator>> errorsAll = new
 * HashMap<>(); errorsAll = new AddErrorArrayError(new
 * ValidateValueComposite().validate(user), errorsAll).getErrorsAll(); errorsAll
 * = new AddErrorArrayError(new ValidateSizeComposite().validate(user),
 * errorsAll).getErrorsAll(); return errorsAll; }
 * 
 * public static HashMap<String, ArrayList<ErrorValidator>> getErrorsValue(User
 * user) { HashMap<String, ArrayList<ErrorValidator>> errorsAll = new
 * HashMap<>(); errorsAll = new AddErrorArrayError(new
 * ValidateValueComposite().validate(user), errorsAll).getErrorsAll(); return
 * errorsAll; }
 * 
 * public static HashMap<String, ArrayList<ErrorValidator>> getErrorsSize(User
 * user) { HashMap<String, ArrayList<ErrorValidator>> errorsAll = new
 * HashMap<>(); errorsAll = new AddErrorArrayError(new
 * ValidateSizeComposite().validate(user), errorsAll).getErrorsAll(); return
 * errorsAll; }
 * 
 * public static HashMap<String, ArrayList<ErrorValidator>>
 * getErrorsAll(UserData user) { HashMap<String, ArrayList<ErrorValidator>>
 * errorsAll = new HashMap<>(); errorsAll = new AddErrorArrayError(new
 * ValidateValueComposite().validate(user), errorsAll).getErrorsAll(); errorsAll
 * = new AddErrorArrayError(new ValidateSizeComposite().validate(user),
 * errorsAll).getErrorsAll(); return errorsAll; }
 * 
 * }
 */