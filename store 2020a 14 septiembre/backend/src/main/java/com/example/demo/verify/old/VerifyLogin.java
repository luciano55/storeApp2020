package com.example.demo.verify.old;
/*
 * import harnina.model.procedure.CallerUser;
 * 
 * import java.sql.SQLException; import java.util.HashMap;
 * 
 * public class VerifyLogin { CallerUser callerUser = null; harnina.entity.Login
 * login;
 * 
 * public VerifyLogin(harnina.entity.Login login) throws SQLException,
 * ClassNotFoundException { callerUser = new CallerUser(); this.login = login; }
 * public HashMap<String, StateVerify> verify() throws SQLException {
 * 
 * HashMap<String, StateVerify> verifyHashMap = new HashMap<String,
 * StateVerify>();
 * 
 * if(callerUser.existUserUser(this.login.getUser())){
 * verifyHashMap.put(this.login.getUser(), StateVerify.STATE_USEREXITS); }
 * if(callerUser.existPasswordUser(this.login.getPassword())){
 * verifyHashMap.put(this.login.getPassword(), StateVerify.STATE_PASSWORDEXITS);
 * } return verifyHashMap; } }
 * 
 */