package com.example.demo.verify.old;
/*
 * import com.google.gson.Gson; import harnina.entity.Login; import
 * harnina.entity.User;
 * 
 * import java.sql.SQLException; import java.util.HashMap;
 * 
 * public class Verification { Gson gson; public Verification(){ this.gson = new
 * Gson(); }
 * 
 * public HashMap<String, StateVerify> VerificationUser(String data) throws
 * SQLException, ClassNotFoundException { User user = this.gson.fromJson(data,
 * User.class); return (new VerifyUser(user)).verify(); } public HashMap<String,
 * StateVerify> VerificationLogin(String data) throws SQLException,
 * ClassNotFoundException { Login login = this.gson.fromJson(data, Login.class);
 * return (new VerifyLogin(login)).verify(); } public HashMap<String,
 * StateVerify> VerificationLoginExist(String olUser, harnina.entity.Login
 * login) throws SQLException, ClassNotFoundException { return (new
 * VerifyLoginExist(olUser,login)).verify(); } }
 */