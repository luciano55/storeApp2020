package com.example.demo.verify.old;

/*
 * import harnina.model.procedure.CallerUser;
 * 
 * import java.sql.SQLException; import java.util.HashMap;
 * 
 * public class VerifyLoginExist { CallerUser callerUser = null; String olUser;
 * harnina.entity.Login login; public VerifyLoginExist(String olUser,
 * harnina.entity.Login login) throws SQLException, ClassNotFoundException {
 * this.callerUser = new CallerUser(); this.olUser = olUser; this.login = login;
 * 
 * } public HashMap<String, StateVerify> verify() throws SQLException {
 * 
 * HashMap<String, StateVerify> verifyHashMap = new HashMap<String,
 * StateVerify>();
 * 
 * if(callerUser.existOtroUserUser(this.olUser, login.getUser())){
 * verifyHashMap.put(login.getUser(), StateVerify.STATE_USEREXITS); }
 * if(callerUser.existOtroEmailUser(this.olUser, login.getPassword())){
 * verifyHashMap.put(login.getPassword(), StateVerify.STATE_PASSWORDEXITS); }
 * return verifyHashMap; } }
 */