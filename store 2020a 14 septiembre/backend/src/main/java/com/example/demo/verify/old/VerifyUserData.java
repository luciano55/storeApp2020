package com.example.demo.verify.old;
/*
 * import harnina.model.procedure.CallerUser;
 * 
 * import java.sql.SQLException; import java.util.HashMap;
 * 
 * public class VerifyUserData {
 * 
 * CallerUser callerUser = null;
 * 
 * public VerifyUserData() throws SQLException, ClassNotFoundException {
 * callerUser = new CallerUser(); } public HashMap<String, StateVerify>
 * verify(String activeUser, harnina.entity.UserData user) throws SQLException {
 * 
 * HashMap<String, StateVerify> verifyHashMap = new HashMap<String,
 * StateVerify>();
 * 
 * if(callerUser.existOtroNifUser(activeUser, user.getNif())){
 * verifyHashMap.put(user.getNif(), StateVerify.STATE_NIFEXITS); }
 * 
 * if(callerUser.existOtroEmailUser(activeUser, user.getEmail())){
 * verifyHashMap.put(user.getEmail(), StateVerify.STATE_EMAILEXITS); }
 * 
 * return verifyHashMap; } }
 */