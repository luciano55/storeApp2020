package com.example.demo.verify;

/*
 * 
 * import harnina.model.procedure.CallerUser;
 * 
 * import java.sql.SQLException; import java.util.HashMap;
 * 
 * public class VerifyUser { CallerUser callerUser = null; harnina.entity.User
 * user;
 * 
 * public VerifyUser(harnina.entity.User user) throws SQLException,
 * ClassNotFoundException { callerUser = new CallerUser(); this.user = user; }
 * public HashMap<String, StateVerify> verify() throws SQLException {
 * 
 * HashMap<String, StateVerify> verifyHashMap = new HashMap<String,
 * StateVerify>(); if(callerUser.existNifUser(this.user.getNif())){
 * verifyHashMap.put(user.getNif(), StateVerify.STATE_NIFEXITS); }
 * if(callerUser.existEmailUser(this.user.getEmail())){
 * verifyHashMap.put(this.user.getEmail(), StateVerify.STATE_EMAILEXITS); }
 * if(callerUser.existUserUser(user.getUser())){
 * verifyHashMap.put(this.user.getUser(), StateVerify.STATE_USEREXITS); } return
 * verifyHashMap; } }
 */